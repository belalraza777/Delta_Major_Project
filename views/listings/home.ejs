<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Easybnb - Welcome</title>
  <!-- Font Awesome CDN -->
  <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    rel="stylesheet"
  />
  <style>
    /* Reset & base */
    *, *::before, *::after {
      box-sizing: border-box;
    }
    body, html {
      margin: 0; padding: 0; height: 100%;
      background: #fff;
      font-family: 'Poppins', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      color: #2c3e50;
      text-align: center;
      padding: 20px;
    }

    /* Title with icon */
    .title-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      margin-bottom: 10px;
      user-select: none;
      flex-wrap: wrap; /* allows wrapping on narrow screens */
      gap: 10px;
    }
    .title-container i {
      color: #ff385c;
      font-size: 4rem;
      filter: drop-shadow(0 1px 1px rgba(255, 56, 92, 0.3));
      flex-shrink: 0;
    }
    h1 {
      font-weight: 700;
      font-size: 3.5rem;
      letter-spacing: 0.07em;
      margin: 0;
      line-height: 1;
      white-space: nowrap; /* prevent line break */
    }

    /* Subtitle */
    p.subtitle {
      font-weight: 500;
      font-size: 1.2rem;
      color: #7f8c9a;
      margin-bottom: 3em;
      max-width: 90vw;
      width: 380px;
      line-height: 1.4;
      user-select: none;
    }

    /* Buttons container */
    .btn-group {
      display: flex;
      gap: 18px;
      justify-content: center;
      flex-wrap: wrap;
      max-width: 320px;
      width: 100%;
    }

    /* Buttons */
    .btn {
      flex: 1 1 140px;
      padding: 14px 0;
      font-weight: 600;
      font-size: 1.15rem;
      border-radius: 30px;
      border: none;
      cursor: pointer;
      transition: all 0.3s ease;
      user-select: none;
      text-decoration: none;
      display: inline-block;
      text-align: center;
      min-width: 120px;
    }
    .btn-login {
      background-color: #ff385c;
      color: white;
      box-shadow: 0 6px 15px rgba(255, 56, 92, 0.35);
    }
    .btn-login:hover {
      background-color: #e62e53;
      box-shadow: 0 8px 22px rgba(230, 46, 83, 0.5);
    }

    .btn-signup {
      background-color: transparent;
      border: 2.5px solid #ff385c;
      color: #ff385c;
      box-shadow: none;
    }
    .btn-signup:hover {
      background-color: #ff385c;
      color: white;
      box-shadow: 0 6px 15px rgba(255, 56, 92, 0.35);
    }

    /* Responsive */
    @media (max-width: 460px) {
      h1 {
        font-size: 2.6rem;
      }
      p.subtitle {
        font-size: 1.1rem;
        margin-bottom: 2.5em;
        max-width: 90vw;
        width: auto;
      }
      .btn-group {
        flex-direction: column;
        width: 100%;
        height: 20px;
      }
      .btn {
        flex: 1 1 100%;
        font-size: 1.1rem;
    
      }
      .title-container {
        justify-content: center;
      }
    }
  </style>
</head>
<body>

  <div class="title-container" aria-label="Easybnb logo and title">
    <i class="fa-solid fa-location-dot" aria-hidden="true"></i>
    <h1>EASYBNB</h1>
  </div>

  <p class="subtitle">
    Your simple and reliable way to book amazing stays and experiences worldwide.
  </p>

  <div class="btn-group">
    <a href="/login" class="btn btn-login" role="button">Login</a>
    <a href="/signup" class="btn btn-signup" role="button">Signup</a>
  </div>

</body>
</html>
